{
  "name": "simple-e-commerce",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:customer-frontend\" \"npm run dev:admin-panel\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:admin-panel": "cd admin-panel && npm run dev",
    "dev:customer-frontend": "cd customer-frontend && npm run dev",
    "install:all": "npm install -f && cd customer-frontend && npm install -f && cd ../backend && npm install -f"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
